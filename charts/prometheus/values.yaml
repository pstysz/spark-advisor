fullnameOverride: "spark-advisor-prometheus"

image:
  repository: prom/prometheus
  tag: "v2.53.0"
  pullPolicy: IfNotPresent

config:
  scrapeTarget: "spark-advisor-gateway:8080"

service:
  type: ClusterIP
  port: 9090

resources: {}
