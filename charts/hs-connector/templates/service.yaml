apiVersion: v1
kind: Service
metadata:
  name: {{ include "hs-connector.fullname" . }}
  labels:
    {{- include "hs-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "hs-connector.selectorLabels" . | nindent 4 }}
