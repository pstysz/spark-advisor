[project]
name = "spark-advisor-workspace"
version = "0.1.11"
description = "Workspace root for spark-advisor monorepo"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/*"]
exclude = ["packages/spark-advisor-frontend"]
